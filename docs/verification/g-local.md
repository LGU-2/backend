# G-LOCAL 판정 절차

절차 본문은 **common 저장소**에 있다.

```
<common>/docs/verification/g-local.md
```

세 저장소가 같은 절차로 판정해야 해서 한 벌만 둔다. 복사하면 갈라진다.
`./verify.sh` 가 내는 판정 지시문도 그 경로를 가리킨다.

common 이 어디 있는지는 `./verify.sh` 출력의 `기준` 줄에 찍힌다.
받는 방법은 [verification-guide.md](./verification-guide.md) 의 준비 절에 있다.
